# `.ai/` — Archived

The multi-agent handoff workspace (Cursor/Claude/Antigravity baton-passing) has been archived to `.ai.archive/`. It was a pre-open-source collaboration protocol that is superseded by the `.claude/` agent and command system.

## Current AI development infrastructure

- **`.claude/agents/`** — architect, explorer, implementer, reviewer, encore-expert
- **`.claude/commands/`** — /init, /commit, /code-review, /implement-plan, /research, /validate-and-fix, /cleanup, /factory-sync, /review-branch
- **`.claude/rules/`** — orchestrator behavioral rules
- **`CLAUDE.md`** — project conventions (root, platform/, stagecraft/)
- **`AGENTS.md`** — session protocol and agent registry

## Spec-first workflow

Specs in `specs/` are the source of truth. See `specs/000-bootstrap-spec-system/spec.md` for the constitutional contract.
