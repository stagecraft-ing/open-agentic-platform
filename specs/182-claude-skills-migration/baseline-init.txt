# Baseline /init dispatch manifest — placeholder

This file is established by spec 182 (AC-9) as the structured
manifest of `/init` dispatch behavior against `main`, captured
**before** the migration PR opens.

## Status

PLACEHOLDER. The actual baseline capture happens when spec 182 is
promoted from `draft` to `approved` and the migration is ready to
land. At that point, the captured manifest replaces this file.

Capture procedure (per spec 182 AC-9):

1. From a fresh Claude Code session against `main`, invoke `/init`.
2. Record the dispatched read list (sorted set of repo-relative
   file paths).
3. Record the tool invocations grouped into parallel batches and
   sequential steps per the manifest shape in spec 182 AC-9.
4. Compute `PROTOCOL_SOURCE_HASH` as sha256 over AGENTS.md lines
   from `## New Sessions` heading inclusive to the next `## `
   heading exclusive, trailing whitespace stripped per line, no
   final-newline normalization.
5. Serialize as the structured manifest shape and replace this
   placeholder with the result.

Until that capture lands, this file's presence satisfies spec 182's
`establishes:` claim for the path; its contents are not consulted by
AC-9 verification.
