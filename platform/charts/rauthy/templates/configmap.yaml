apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rauthy.fullname" . }}-config
  labels:
    {{- include "rauthy.labels" . | nindent 4 }}
data:
  config.toml: |
    # Minimal config — most settings provided via environment variables.
    # Rauthy >= 0.35.0 requires this file to exist.
