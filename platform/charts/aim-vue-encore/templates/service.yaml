apiVersion: v1
kind: Service
metadata:
  name: {{ include "aim-vue-encore.fullname" . }}
  labels:
    {{- include "aim-vue-encore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aim-vue-encore.selectorLabels" . | nindent 4 }}
