# Spec Spine Semver Policy

> **TODO** — placeholder per Epic 2 master plan §Out of scope.
>
> Substantive authoring deferred to post-cleanup follow-up. This file
> exists to reserve the canonical location for the spec-spine semver
> policy under `standards/spec/`; the content (when MAJOR/MINOR/PATCH
> bumps apply to the registry schema, how implementations are
> versioned relative to specs, etc.) is authored in a separate spec
> after Epic 2 lands.
>
> See `standards/spec/constitution.md` §Constitutional invariants and
> the spec-compiler's `SPEC_VERSION` constant in
> `tools/spec-compiler/src/lib.rs` for the current de facto policy.
