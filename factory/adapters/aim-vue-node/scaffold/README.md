# AIM Vue+Node Scaffold

This directory is a placeholder. The actual scaffold template lives at:
- Local development: `GovAlta-Pronghorn/template` repository (historically `AIM-vue-node-template`)
- The `factory-run` CLI's `--scaffold-source` flag specifies the template path at runtime

The engine copies the scaffold source into the target project directory before dispatching the s6a-scaffold-init step.
